#include <stdio.h>

int main() {
  double x;

  printf("Nhap vao mot so x: ");
  scanf("%lf", &x);

  
  printf("x^2 = %.2lf\n", x * x);
  printf("x^3 = %.2lf\n", x * x * x);
  printf("x^4 = %.2lf\n", x * x * x * x);

  return 0;
}
